// eslint-disable-next-line no-shadow
enum IPC{

    'DIFF_MAIN_WINDOW' = 'diff-main-window',
    'MIN_MAIN_WINDOW' = 'min-main-window',
    'CLOSE_WINDOW' = 'close-window',
    'EXIT' = 'exit'

}

export default IPC;
